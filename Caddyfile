desk.gen7fuel.com {
    # API — forward to Express (prefix kept; Express routes are mounted under /api)
    handle /api/* {
        reverse_proxy api:5000
    }

    # Everything else — serve the built SPA
    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
